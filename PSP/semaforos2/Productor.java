package semaforos2;
/* 
import java.nio.Buffer;
import java.util.logging.Logger;

public class Productor {
    private int[] buffer;
    private int siguiente;
    private boolean uno;
    //private boolean otro "= false;" no creo que lo usemos aqui XD 
}

public Productor(int tamanio){
    this.buffer = new
}

public synchronized void producir(int i){
    while(){
        try{
            wait();
        } catch (InterruptedException ex){
            Logger.getLogger(Buffer.class.getName());
        }
    }
}*/